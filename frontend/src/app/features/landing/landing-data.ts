import { aboutParagraphs, experienceCards, landingStats, socialLinks } from '../../core/site-data';

export const landingContent = {
  aboutParagraphs,
  experienceCards,
  landingStats,
  socialLinks,
} as const;

